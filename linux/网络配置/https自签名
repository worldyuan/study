# 安装openssl
```
sudo apt install openssl
```

# 创建自签名ssl证书
```
openssl req -newkey rsa:4096 \
            -x509 \
            -sha256 \
            -days 3650 \
            -nodes \
            -out example.crt \
            -keyout example.key
```
